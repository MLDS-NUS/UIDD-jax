"""
# UIDD package

This package implements the basic routines for building and training
(with or without model reduction/closure modelling)
UIDD and variants.

## Main Modules
- `UIDD.dynamics`: Dynamic models, including SDEs, UIDD, and variants
- `UIDD.transformations`: Transformations for dimensionality reduction and reconstruction
- `UIDD.trainers`: Training routines for SDEs and those with dimensionality transformations
- `UIDD.models`: Basic model definitions (mostly fully connected neural networks)

"""
